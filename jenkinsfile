pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'env'
                sh 'git diff --name-only $GIT_PREVIOUS_COMMIT $GIT_COMMIT ui'
            }
        }
    }
}
